import React, { FunctionComponent } from 'react'

const FunctionAsComponent: FunctionComponent = (): React.ReactElement => {
  return <div>FunctionComponent</div>
}

export default FunctionAsComponent
